var numero1 = 5
let numero2 = 10 //O let é igual um var, a única diferença é que o var é visto de um escopo global, já o let é vista apenas de um escopo local
const numero3 = 15 //O const é uma constante, uma vez que foi atribuido um valor a ele, você não pode alterar

//Entre let e var, o recomendado é usar sempre let, pois ao usar let você não precisa se preocupar se usou o nome para uma variável antes, já que a variável
//existe somente dentro daqule esocpo